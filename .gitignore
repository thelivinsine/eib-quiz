# Local-only scratch: source PDFs, notes, sandbox clones. Never committed.
local/
work/

# Guard against loose PDFs landing back at the repo root
# (the real catalogue is tracked at img/gesamtfragenkatalog-lebenindeutschland.pdf)
/*.pdf

# Per-machine Claude Code settings; the launch config is shared.
.claude/settings.local.json

# Superseded mockup material: the README explains why these are dead, so it is
# tracked; the 11MB of crops behind it is local-only and not worth git history.
docs/Mockups/archive/*
!docs/Mockups/archive/README.md
